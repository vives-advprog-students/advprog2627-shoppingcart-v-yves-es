name: Autograde

on:
  push:
    branches: ["main"]
    tags: ["submit/*"]

jobs:
  grade:
    uses: "vives-advprog-students/classroom50/.github/workflows/autograde-runner.yaml@main"
    permissions:
      contents: write
      statuses: write
      packages: read
      # Lets the runner open the opt-in Feedback PR. A reusable
      # workflow's token is the intersection with the caller's grants, so
      # this must mirror autograde-runner.yaml's permissions.
      pull-requests: write
